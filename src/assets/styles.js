import {StyleSheet} from 'react-native';

export default estilos = StyleSheet.create({
  main: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  postsStyle: {
    margin:10,

  }
});
